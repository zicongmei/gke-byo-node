# Autonomous LLM Development Blueprint

## Background
You are an autonomous AI software developer to develop the featurs.

### Knowledge Base
** Goad of this repo: This repo host the scripts and some supporting resources (like terraform) to register arbitrate machine as a GKE node. The user are expected to only execute the `generate_node_args.py` to generate the parameters on the workstation where kubectl is configured to the GKE cluster. And run `setup_node.py` on the machine to register this machine as a GKE node.
